%tree{:id => '0'}
  %item{:id => '/', :text => "#{@project.name}"}
    - @project.files.each do |file|
      %item{:id => file.path, :text => file.name, :child => if file.stat.directory? then 1 else 0 end}
